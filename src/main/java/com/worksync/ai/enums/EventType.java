package com.worksync.ai.enums;

/**
 * Enum representing different types of events in the WorkSync system.
 */
public enum EventType {
    APP_USAGE,    // For application usage tracking events
    SECURITY,     // For security-related events
    ALERT         // For system alerts and notifications
} 